@using MyCommune.Models.ViewModel.User;
@model List<MyCommune.Models.ViewModel.User.UserDetailViewModel>

<div class="card">
    <div class="card-header">
        User Details
    </div>

    <div class="card-body">
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Name</th>
                    <th scope="col">Mobile</th>
                    <th scope="col">Email</th>
                    <th scope="col">Address</th>
                </tr>
            </thead>
            <tbody>
                @{
                    uint i = 1;
                    foreach (var user in Model)
                    {
                        <tr>
                            <th scope="row">
                                @(i++)
                            </th>
                            <th scope="row">
                                @($"{user.FirstName} {user.LastName}")
                            </th>
                            <th scope="row">
                                <a asp-controller="User" asp-action="GetView" asp-route-id="@user.Id">@user.Mobile</a>
                            </th>
                            <th scope="row">@user.Email</th>
                            <th scope="row">@user.Address</th>

                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>


    @* <div class="card-body">
    <div class="mb-3 row">
    <label for="staticEmail" class="col-sm-2 col-form-label">Name</label>
    <div class="col-sm-10">
    <input readonly class="form-control-plaintext" asp-for="@user.Name">
    </div>
    </div>
    <div class="mb-3 row">
    <label for="staticEmail" class="col-sm-2 col-form-label">Mobile</label>
    <div class="col-sm-10">
    <input readonly class="form-control-plaintext" asp-for="@user.Mobile">
    </div>
    </div>
    <div class="mb-3 row">
    <label for="staticEmail" class="col-sm-2 col-form-label">Email</label>
    <div class="col-sm-10">
    <input readonly class="form-control-plaintext" asp-for="@user.Email">
    </div>
    </div>
    <div class="mb-3 row">
    <label for="staticEmail" class="col-sm-2 col-form-label">Address</label>
    <div class="col-sm-10">
    <input readonly class="form-control-plaintext" asp-for="@user.Address">
    </div>
    </div>
    </div>*@
</div>